{
    "version": "0.2.0",
    "configurations": [
      {
        "type": "chrome",
        "request": "launch",
        "name": "Launch Chrome Server",
        "url": "http://localhost:3000", // Change this to your application's URL
        "webRoot": "${workspaceFolder}"
      }
    ]
  }
  